{
    "sql": "SELECT * FROM (SELECT *, LAG(__time, 10) OVER (PARTITION BY UserId ORDER BY __time) AS pt1 FROM \"datasource\" WHERE Endpoint = '/foo') AS __inner WHERE pt1 IS NOT NULL AND __time - pt1 <= 600",
    "type": "sql"
}